<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeWithBottomMenu" parent="AppTheme">
        <item name="android:windowActionBarOverlay" >true</item>
    </style>
</resources>
